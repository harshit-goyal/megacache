"""Python SDK examples and conformance tests.

The installable SDK source and metadata live in ``src/megacache`` and the
repository root because the Python SDK is also the dependency-free server
package.
"""
